"""Online sibling preference DPO with spatial (changed-residue) support.

Phase-scoped package for the online-preference task book: sibling data
generation is reused from ``vhh_rl.branch_distill``; the training objective is
always the standard reference-relative Diffusion-DPO.
"""
