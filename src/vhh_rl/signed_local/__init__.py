"""SL-CF-DPO: Signed Local Counterfactual Diffusion-DPO (task book).

Submodules are imported explicitly; this package deliberately keeps an empty
namespace so that CPU-only environments (CI) can import light submodules
without pulling in the boltzgen-dependent native stack.
"""
