"""On-policy sibling branching + changed-residue local distillation.

Feasibility task book work name: branch_distill.  Submodules are imported
explicitly so CPU-only environments can import the pure logic without the
BoltzGen-dependent heavy paths.
"""
